
урок 2
toString
toPrecign
toFixed


Number() string to number (not intelectual)
parseInt('13jljlkj') = 13


== приведение к одному типу потом сравнение - приведет объекты к одному типу потом сравнит
=== не делает приведение типов а сразу сравнивает

объект = набор именованых свойств
in - проверка есть ли такое свойство у объекта

in для массива - проверка наличия индекса
массивы = объекты

instanceof проверка объекта - является ли экземпляром

x?y:z
typeof

валидация ис нан?
2 числа вводится вывести большее
3 числа
число - вывести сумму разрядов 123=6
ввод - размер стороны квадрата - нарисовать квадрат звездочками
===
arr of ints print all elements 0-10
-//- replace all objects to numbers
-//- print quantity of simple digits
=====
write function accepting array of ins  - revert second part of array
-//- and indexes of arrays, sort elements indexes of which given
========

Document modification
• You can create new Element nodes with the createElement()
method of the Document object
• Once you have a new node, you can insert it into the
document with the Node methods appendChild() or
insertBefore()
• The removeChild() method removes a node from the
document tree
• The Element type also defines get Attribute() and
setAttribute() methods that you can use to query and set
HTML attributes
// Create a < script> elem ent
var s = docum ent.createElement("script");
var headline = docum ent.getElem entById("headline");
headline .setAttribute("align", "center");


200-299 - ok
300-399 redirect
400-499 client error
500-599 server error
